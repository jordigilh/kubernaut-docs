name: Deploy Documentation

on:
  push:
    branches: [main]
  repository_dispatch:
    types: [crd-docs-updated]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Fetch generated CRD API reference
        run: |
          curl -fsSL \
            https://raw.githubusercontent.com/jordigilh/kubernaut/main/docs/generated/crds.md \
            -o docs/api-reference/crds.md \
          || echo "WARNING: CRD reference not available on main yet, skipping"

      - run: mkdocs build --strict

      - uses: actions/upload-pages-artifact@v4
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
